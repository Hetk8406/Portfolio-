# Portfolio - Het Kikani

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

## Project Structure
- `src/components`: React components for each section.
- `userProfileData.json`: Data source for the portfolio content.

## Getting Started
1. Clone the repo
2. Run `npm install`
3. Run `npm run dev`
